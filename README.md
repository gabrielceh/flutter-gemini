# gemini_app

## DEV

1. Clonar el archivo `.env.template` y renombrarlo a `.env` con sus valores.
2. Ejecutar el comando cada para acutalizar nuestros providers cuando hayan cambios en el archivo:

```dart
dart run build_runner watch -d
```
